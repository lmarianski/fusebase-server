{
  "name": "fusebase-server",
  "version": "1.0.0",
  "description": "A C&C server made in nodejs",
  "main": "src/index.js",
  "scripts": {
	"start": "npm run build && node .",
	"dev": "nodemon --exec \"cd ./src/control_panel && npm run buildDev && cd ../.. && node .\" -i *dist* -i modulesSettings.json -i serverSettings.json",
	"test": "echo \"Error: no test specified\" && exit 1",
	"pullModules": "git submodule update --remote",
	"build": "cd ./src/control_panel && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lukas2005/fusebase-server.git"
  },
  "bugs": "https://github.com/lukas2005/fusebase-server/issues",
  "author": "lukas2005",
  "license": "GPL-3.0",
  "dependencies": {
    "express": "^4.17.1",
    "ipaddr.js": "^1.9.0",
    "javascript-obfuscator": "^0.18.1",
    "node-cleanup": "^2.1.2",
    "nosql": "^6.1.0",
    "request": "^2.88.0",
    "socket.io": "^2.2.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.1"
  }
}
